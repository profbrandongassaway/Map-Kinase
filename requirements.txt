# Map-Kinase runtime dependencies (production web deployment).
# Pinned versions are based on the currently used app/runtime imports.
shiny==1.5.1
uvicorn==0.40.0
pandas==2.3.3
numpy==2.4.6
requests==2.33.0
Pillow==12.2.0
pywikipathways==0.0.3
python-multipart==0.0.27
pydantic>=2.0